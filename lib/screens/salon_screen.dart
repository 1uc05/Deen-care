import 'package:flutter/material.dart';

class SalonScreen extends StatelessWidget {
  const SalonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Salon'),
      ),
      body: const Center(
        child: Text('Écran salon - À implémenter'),
      ),
    );
  }
}
